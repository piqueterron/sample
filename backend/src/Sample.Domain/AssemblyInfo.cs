namespace Sample.Domain;

/// <summary>
/// Marker type used by assembly scanning (e.g. Mediator's
/// <c>options.Assemblies = [typeof(Domain.AssemblyInfo).Assembly]</c>) to
/// locate the <c>Sample.Domain</c> assembly without requiring a public type
/// inside it.
/// </summary>
public static class AssemblyInfo
{
}
